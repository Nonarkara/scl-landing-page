# SCL — desktop hero + nav + alumni-in-news (2026-06-03)

## Requests from Dr Non
- [ ] 1. Hero looks great on mobile but AWKWARD on desktop/web — rebalance desktop composition
- [ ] 2. IndexTeaser ("เครื่องมือวัดผลว่าเมืองไหน 'อัจฉริยะ' จริง" / SCITI teaser) is stale vs
      sciti.nonarkara.org — DECIDE: leave or update to match page dynamism (my call)
- [ ] 3. Nav check: does "Format & Timeline" (/?tab=journey) and the far-left logo
      (Smart City Leadership → /) go to the same place? Investigate + fix any real dup/confusion
- [ ] 4. Re-verify alumni data + stats. Stats are good; consider adding more dimensions
- [ ] 5. NEW: notable alumni (governors/mayors/CEOs) — hyperlink one name each to a REAL recent
      news mention so visitors see what alumni are doing now. REAL URLs only, verify each resolves.

## Notes
- Mobile-first is correct (§11.8); desktop must scale up cleanly, not look empty/awkward
- REAL news URLs only — no fabrication (§11.4 + lessons). Verify every link.
